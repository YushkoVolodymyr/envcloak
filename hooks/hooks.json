{
  "description": "envcloak: block raw access to real .env files (Read/Edit/Write/Grep/Glob/Bash). Secrets must be read through the envcloak MCP (env_read), which returns a masked, length-preserving view. Example/template files (.env.example, .env.sample, .env.template, .env.dist, .env.schema) stay readable.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/tools/resolve-python.sh\"",
            "timeout": 15
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Read|Edit|Write|MultiEdit|NotebookEdit|Grep|Glob|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${ENVCLOAK_PYTHON:-python3}\" \"${CLAUDE_PLUGIN_ROOT}/block-env-files.py\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
